version: "3.9"
services:
  backend:
    volumes:
      - ./doctor-appointment-backend:/app
      - /app/node_modules
    command: npm run dev

  frontend:
    volumes:
      - ./doctor-appointment-frontend:/app
      - /app/node_modules
    command: npm run dev
    ports:
      - "5173:5173"
